# 🚨 КРИТИЧЕСКАЯ ИНСТРУКЦИЯ ПО СИНХРОНИЗАЦИИ РЕПОЗИТОРИЕВ

## ⚠️ ВАЖНО: ВСЕГДА ЧИТАЙ ЭТО ПЕРЕД ДОБАВЛЕНИЕМ НОВОГО РЕПОЗИТОРИЯ!

## 📋 Проблема, которая была:
При добавлении нового репозитория `opening` в конструктор, скрипт синхронизации **НЕ БЫЛ ОБНОВЛЕН**, поэтому изменения из этого репозитория **НЕ ПРИМЕНЯЛИСЬ АВТОМАТИЧЕСКИ**.

## ✅ Что нужно делать при добавлении нового репозитория:

### 1. Обновить `config/repos.json`
Добавить новый репозиторий в список:
```json
{
  "name": "название-репо",
  "url": "https://github.com/kikanbig/название-репо.git",
  "description": "Описание",
  "route": "/путь",
  "localPath": "../путь-к-репо",
  "components": ["Component1.tsx", "Component2.tsx"],
  "pages": [{"source": "Index.tsx", "target": "ЦелеваяСтраница.tsx"}],
  "data": ["data.ts"],
  "assets": true
}
```

### 2. ⚠️ КРИТИЧЕСКИ ВАЖНО: Обновить `scripts/sync-repo.sh`

#### 2.1. Добавить путь к репозиторию в секцию case (строки ~80-92):
```bash
"название-репо")
    REPO_PATH="../путь-к-репо"
    ;;
```

#### 2.2. Добавить логику копирования в секцию case (строки ~114-180):
```bash
"название-репо")
    echo "📋 Копирование файлов из название-репо..."
    # Копируем компоненты
    echo "🔧 Копирование компонентов..."
    cp "$REPO_PATH/src/components/Component1.tsx" src/components/ 2>/dev/null || true
    cp "$REPO_PATH/src/components/Component2.tsx" src/components/ 2>/dev/null || true
    echo "  ✅ Компоненты скопированы"
    
    # Копируем страницу
    echo "📄 Копирование страницы..."
    cp "$REPO_PATH/src/pages/Index.tsx" src/pages/ЦелеваяСтраница.tsx 2>/dev/null || true
    echo "  ✅ Index.tsx → ЦелеваяСтраница.tsx"
    
    # Копируем ассеты
    echo "🖼️ Копирование ассетов..."
    cp -r "$REPO_PATH/src/assets/"* src/assets/ 2>/dev/null || true
    cp -r "$REPO_PATH/public/"* public/ 2>/dev/null || true
    echo "  ✅ Ассеты скопированы"
    ;;
```

### 3. Добавить скрипты синхронизации в `package.json`
```json
"sync:название-репо": "./scripts/sync-repo.sh название-репо",
"sync:название-репо-diff": "./scripts/sync-with-diff.sh название-репо",
```

### 4. Обновить `sync:all` в `package.json`
Добавить новый репозиторий в цепочку:
```json
"sync:all": "npm run sync:hoff && npm run sync:sets && npm run sync:opening && npm run sync:название-репо && npm run build && railway up"
```

**⚠️ ВАЖНО**: Не забудь добавить репозиторий в список доступных в `scripts/sync-repo.sh` (строка ~91):
```bash
echo "Доступные: hoff-divan-insights, sets-repo, opening, regions, название-репо"
```

### 5. Добавить роут в `src/App.tsx`
```tsx
import НоваяСтраница from "./pages/НоваяСтраница";

// В Routes:
<Route path="/путь" element={<НоваяСтраница />} />
```

### 6. Протестировать синхронизацию
```bash
npm run sync:название-репо
npm run build
npm run dev
```

### 7. Задеплоить
```bash
git add -A
git commit -m "✨ Добавлен новый репозиторий: название-репо"
git push origin main
railway up
```

## 🔄 Как проверить, что синхронизация работает:

1. Сделай изменение в исходном репозитории на Lovable
2. Запусти `npm run sync:all`
3. **ВАЖНО**: Проверь, что изменения применились в файлах:
   - Открой измененные файлы в редакторе
   - Убедись, что новый код присутствует
   - Проверь, что не осталось старых версий
4. Убедись, что проект собирается: `npm run build`
5. Задеплой на Railway: `railway up`

## ⚠️ Частые проблемы при синхронизации:

### Проблема 1: Изменения не применяются
**Причина**: Скрипт `sync-repo.sh` не обновлен для нового репозитория

**Решение**: 
1. Проверь, что в `scripts/sync-repo.sh` есть секция для твоего репозитория
2. Убедись, что все нужные файлы копируются
3. Запусти синхронизацию вручную: `npm run sync:название-репо`

### Проблема 2: Старая версия компонента после синхронизации
**Причина**: Компонент не указан в списке копируемых файлов

**Решение**:
1. Открой `scripts/sync-repo.sh`
2. Найди секцию для твоего репозитория
3. Добавь копирование нужного файла:
   ```bash
   cp "$REPO_PATH/src/components/НужныйКомпонент.tsx" src/components/ 2>/dev/null || true
   ```

### Проблема 3: Цвета не применились
**Причина**: `index.css` не копируется из исходного репозитория

**Решение**: 
- Цвета должны быть в основном `src/index.css` проекта
- Если нужны специфичные цвета для страницы - используй inline стили или отдельный CSS файл

### Проблема 4: YouTube видео не отображается (показывается старый плеер)
**Причина**: Компонент не был полностью синхронизирован из исходного репозитория

**Пример**: В репозитории `opening` был коммит `77857ea "Replace video with YouTube embed"`, но изменения не применились

**Решение**:
1. Проверь последние коммиты в исходном репозитории:
   ```bash
   cd ../opening
   git log --oneline -10
   ```
2. Вручную скопируй нужный компонент:
   ```bash
   cd ../21vek-dom.ru
   cp ../opening/src/components/VideoSection.tsx src/components/
   ```
3. Проверь, что изменения применились (открой файл в редакторе)
4. Пересобери и задеплой:
   ```bash
   npm run build
   git add -A
   git commit -m "🎥 Синхронизирован VideoSection с YouTube embed"
   git push origin main
   railway up
   ```

### Проблема 5: После `npm run sync:all` изменения не видны
**Причина**: Скрипт выполнился, но файлы не обновились

**Решение**:
1. Проверь, что в исходном репозитории есть изменения:
   ```bash
   cd ../название-репо
   git pull origin main
   git log --oneline -5
   ```
2. Проверь, что скрипт `sync-repo.sh` копирует нужные файлы
3. Запусти синхронизацию конкретного репозитория с выводом:
   ```bash
   npm run sync:название-репо
   ```
4. Если файлы не копируются - обнови секцию в `scripts/sync-repo.sh`

### ⚠️ Проблема 6: После синхронизации `/regions` или `/opening` показывают кнопки "Мебель, Сеты, Лоты, Акции"
**Причина**: При копировании `Landing.tsx → Regions.tsx` или `Index.tsx → Opening.tsx` ПЕРЕЗАПИСЫВАЕТСЯ импорт Header, и страница начинает использовать **общий Header** (с навигацией) вместо кастомного (без навигации)

**КРИТИЧЕСКОЕ ПОНИМАНИЕ**:
- Репозитории `regions` и `opening` имеют **упрощенный Header БЕЗ навигационных кнопок**
- При синхронизации мы копируем их Header.tsx как `RegionsHeader.tsx` и `OpeningHeader.tsx`
- НО! Мы также копируем страницы, которые импортируют `import { Header } from '@/components/Header'`
- Это приводит к использованию **ОБЩЕГО** Header (с навигацией) вместо кастомного!

**Решение**:
Скрипт `scripts/apply-modifications.sh` автоматически исправляет импорты после синхронизации:
1. Переименовывает `export const Header` → `export const RegionsHeader` в `RegionsHeader.tsx`
2. Меняет импорт в `Regions.tsx`: `Header` → `RegionsHeader`
3. Меняет использование в `Regions.tsx`: `<Header />` → `<RegionsHeader />`
4. То же самое для `opening`

**Если проблема возникла**:
1. Запусти модификации вручную:
   ```bash
   ./scripts/apply-modifications.sh
   ```
2. Проверь файлы:
   ```bash
   # Regions.tsx должен импортировать RegionsHeader:
   grep "import.*Header" src/pages/Regions.tsx
   # Должно быть: import { RegionsHeader } from '@/components/RegionsHeader';
   
   # Opening.tsx должен импортировать OpeningHeader:
   grep "import.*Header" src/pages/Opening.tsx
   # Должно быть: import { OpeningHeader } from '@/components/OpeningHeader';
   ```
3. Если импорты неправильные - скрипт `apply-modifications.sh` не сработал. Проверь его права:
   ```bash
   chmod +x scripts/apply-modifications.sh
   ```
4. Пересобери и задеплой:
   ```bash
   npm run build
   git add -A
   git commit -m "🔧 Исправлены импорты Header для regions/opening"
   git push origin main
   railway up
   ```

**ВАЖНО ДЛЯ БУДУЩИХ СИНХРОНИЗАЦИЙ**:
- ВСЕГДА проверяй после `npm run sync:all`, что `/regions` и `/opening` НЕ показывают кнопки навигации!
- Скрипт `apply-modifications.sh` должен запускаться автоматически после каждой синхронизации
- Если видишь навигацию - значит модификации не применились, запусти `./scripts/apply-modifications.sh` вручную

## 🚫 Что НЕ ДЕЛАТЬ:

- ❌ НЕ добавляй репозиторий только в `config/repos.json` без обновления `sync-repo.sh`
- ❌ НЕ забывай добавлять скрипты в `package.json`
- ❌ НЕ забывай обновлять `sync:all`
- ❌ НЕ пропускай тестирование перед деплоем

## 📝 Текущие репозитории в конструкторе:

1. **hoff-divan-insights** (`/`) - Главная страница
   - Компоненты: Header, HeroSection, CategoryGrid, ProductsSection, Footer, NavLink, ProductCard
   - Путь: `../hoff-divan-insights`

2. **sets-repo** (`/sets`) - Страница сетов
   - Компоненты: SetCard, TopHeader, BannerCarousel
   - Страница: Index.tsx → Sets.tsx
   - Путь: `../repos/sets-repo`

3. **opening** (`/opening`) - Страница открытия магазина
   - Компоненты: VideoSection, PhotoGallery, OpeningHeader
   - Страница: Index.tsx → Opening.tsx
   - Путь: `../opening`

## 🎯 Быстрая команда для синхронизации всех репозиториев:

```bash
npm run sync:all
```

Эта команда:
1. Синхронизирует все репозитории
2. Собирает проект
3. Автоматически деплоит на Railway

---

**ЗАПОМНИ**: При добавлении нового репозитория ВСЕГДА обновляй `scripts/sync-repo.sh`! Это критически важно для работы конструктора!

