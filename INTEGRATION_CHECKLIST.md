# ✅ Чек-лист интеграции нового репозитория в конструктор 21vek-dom.ru

## 🎯 ВАЖНО! Обязательные шаги при добавлении нового репозитория

Каждый раз, когда добавляется новый репозиторий в конструктор, необходимо выполнить **ВСЕ** следующие шаги:

---

## 📋 Полный чек-лист интеграции

### 1️⃣ Клонирование репозитория
```bash
cd "/Users/kikanbig/Контент21/СЕТЫ/Сет вкусный"
git clone <URL-репозитория> <имя-папки>
```

**Пример:**
```bash
git clone https://github.com/kikanbig/opening.git
```

---

### 2️⃣ Копирование файлов в основной проект

```bash
cd 21vek-dom.ru

# Копирование компонентов
cp -r ../<имя-репо>/src/components/* src/components/

# Копирование страницы (переименовать Index.tsx в название страницы)
cp ../<имя-репо>/src/pages/Index.tsx src/pages/<НазваниеСтраницы>.tsx

# Копирование ассетов
cp -r ../<имя-репо>/src/assets/* src/assets/

# Если есть данные
cp -r ../<имя-репо>/src/data/* src/data/

# Если есть видео/медиа в public
cp -r ../<имя-репо>/public/* public/
```

**Пример для opening:**
```bash
cp -r ../opening/src/components/PhotoGallery.tsx src/components/
cp -r ../opening/src/components/VideoSection.tsx src/components/
cp ../opening/src/pages/Index.tsx src/pages/Opening.tsx
cp -r ../opening/src/assets/* src/assets/
cp -r ../opening/public/videos/* public/videos/
```

---

### 3️⃣ Добавление маршрута в App.tsx

**Файл:** `src/App.tsx`

```typescript
// 1. Добавить импорт страницы
import Opening from "./pages/Opening";

// 2. Добавить Route в Routes
<Routes>
  <Route path="/" element={<Index />} />
  <Route path="/sets" element={<Sets />} />
  <Route path="/opening" element={<Opening />} />  {/* ← НОВЫЙ МАРШРУТ */}
</Routes>
```

---

### 4️⃣ ⚠️ КРИТИЧЕСКИ ВАЖНО: Добавление в config/repos.json

**Файл:** `config/repos.json`

```json
{
  "repositories": [
    {
      "name": "opening",
      "url": "https://github.com/kikanbig/opening.git",
      "description": "Страница открытия магазина с видео и фотогалереей",
      "route": "/opening",
      "localPath": "../opening",
      "components": ["PhotoGallery.tsx", "VideoSection.tsx"],
      "pages": [{"source": "Index.tsx", "target": "Opening.tsx"}],
      "data": [],
      "assets": true
    }
  ]
}
```

**Параметры:**
- `name` - короткое имя репозитория (для команд sync)
- `url` - полный URL GitHub репозитория
- `description` - описание страницы
- `route` - маршрут на сайте (например `/opening`)
- `localPath` - путь к клонированному репозиторию
- `components` - список компонентов для синхронизации
- `pages` - маппинг страниц (source → target)
- `data` - список файлов данных
- `assets` - `true` если нужно копировать ассеты

---

### 5️⃣ ⚠️ КРИТИЧЕСКИ ВАЖНО: Добавление скриптов в package.json

**Файл:** `package.json`

```json
{
  "scripts": {
    "sync:hoff": "./scripts/sync-repo.sh hoff-divan-insights",
    "sync:sets": "./scripts/sync-repo.sh sets-repo",
    "sync:opening": "./scripts/sync-repo.sh opening",  // ← ДОБАВИТЬ
    
    "sync:all": "npm run sync:hoff && npm run sync:sets && npm run sync:opening && npm run build && railway up",  // ← ОБНОВИТЬ
    
    "sync:hoff-diff": "./scripts/sync-with-diff.sh hoff-divan-insights",
    "sync:sets-diff": "./scripts/sync-with-diff.sh sets-repo",
    "sync:opening-diff": "./scripts/sync-with-diff.sh opening"  // ← ДОБАВИТЬ
  }
}
```

**Что добавить:**
1. `sync:<имя-репо>` - команда для синхронизации конкретного репозитория
2. Обновить `sync:all` - добавить `&& npm run sync:<имя-репо>`
3. `sync:<имя-репо>-diff` - команда для синхронизации с просмотром изменений

---

### 6️⃣ Обновление документации

#### PROJECT_RULES.md

Добавить в раздел "Подключенные репозитории":

```markdown
#### N. Название страницы - `имя-репо`
- **GitHub**: https://github.com/kikanbig/<имя-репо>.git
- **Статус**: ✅ Интегрирован
- **Описание**: Описание страницы
- **Маршрут**: `/маршрут`
```

#### README.md

Обновить таблицу интегрированных репозиториев:

```markdown
| Репозиторий | Статус | Маршрут | Описание |
|-------------|--------|---------|----------|
| [opening](https://github.com/kikanbig/opening.git) | ✅ Интегрирован | `/opening` | Страница открытия магазина |
```

---

### 7️⃣ Сборка и тестирование

```bash
# Установить зависимости (если добавлялись новые)
npm install

# Собрать проект
npm run build

# Проверить локально
npm run dev
```

---

### 8️⃣ Деплой на Railway

```bash
# Деплой
railway up

# Или полная синхронизация + деплой
npm run sync:all
```

---

### 9️⃣ Коммит изменений

```bash
git add .
git commit -m "✨ Добавлена страница <Название> (/<маршрут>) из нового репозитория"
git push origin main
```

---

## 🔄 Проверка синхронизации

После интеграции проверьте, что репозиторий синхронизируется:

```bash
# Проверить список репозиториев
npm run sync:list

# Проверить статус
npm run sync:status

# Синхронизировать новый репозиторий
npm run sync:<имя-репо>

# Синхронизировать все
npm run sync:all
```

---

## ❌ Частые ошибки

### Ошибка 1: Репозиторий не синхронизируется
**Причина:** Не добавлен скрипт в `package.json`
**Решение:** Добавить `"sync:<имя>": "./scripts/sync-repo.sh <имя>"`

### Ошибка 2: Репозиторий не включен в sync:all
**Причина:** Не обновлена команда `sync:all` в `package.json`
**Решение:** Добавить `&& npm run sync:<имя>` в команду `sync:all`

### Ошибка 3: Скрипт синхронизации не находит репозиторий
**Причина:** Не добавлен в `config/repos.json`
**Решение:** Добавить конфигурацию репозитория в `config/repos.json`

### Ошибка 4: Страница не отображается
**Причина:** Не добавлен Route в `App.tsx`
**Решение:** Добавить импорт и `<Route>` в `src/App.tsx`

---

## 📝 Быстрая шпаргалка

```bash
# 1. Клонировать репозиторий
git clone <URL> <имя>

# 2. Скопировать файлы
cp -r ../<имя>/src/components/* src/components/
cp ../<имя>/src/pages/Index.tsx src/pages/<Название>.tsx
cp -r ../<имя>/src/assets/* src/assets/

# 3. Добавить Route в src/App.tsx
# 4. Добавить в config/repos.json
# 5. Добавить скрипты в package.json:
#    - sync:<имя>
#    - sync:<имя>-diff
#    - обновить sync:all
# 6. Обновить PROJECT_RULES.md и README.md
# 7. npm run build
# 8. railway up
# 9. git commit + push
```

---

## 🎯 Итоговый чек-лист

- [ ] Репозиторий клонирован
- [ ] Файлы скопированы в проект
- [ ] Route добавлен в `src/App.tsx`
- [ ] ⚠️ **Репозиторий добавлен в `config/repos.json`**
- [ ] ⚠️ **Скрипты добавлены в `package.json`**
- [ ] ⚠️ **Команда `sync:all` обновлена**
- [ ] Документация обновлена (PROJECT_RULES.md, README.md)
- [ ] Проект собран (`npm run build`)
- [ ] Проект задеплоен (`railway up`)
- [ ] Изменения закоммичены и запушены
- [ ] Синхронизация работает (`npm run sync:<имя>`)

---

## 🚨 ЗАПОМНИТЬ

**Каждый новый репозиторий = 3 обязательных файла:**

1. ✅ `config/repos.json` - конфигурация репозитория
2. ✅ `package.json` - скрипты синхронизации
3. ✅ `src/App.tsx` - маршрутизация

**Без этих 3 шагов репозиторий НЕ БУДЕТ синхронизироваться!**

