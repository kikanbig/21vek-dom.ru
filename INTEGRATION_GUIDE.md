# 🔧 Руководство по интеграции новых репозиториев

## Общая концепция
Проект `21vek-dom.ru` — это **конструктор страниц** из разных репозиториев. Каждый репозиторий становится отдельной страницей на сайте `dom.21vek.by`.

---

## 📋 Пошаговая инструкция интеграции

### 1️⃣ Клонирование репозитория
```bash
cd "/Users/kikanbig/Контент21/СЕТЫ/Сет вкусный"
git clone <URL_РЕПОЗИТОРИЯ>
```

### 2️⃣ Изучение структуры
Определите:
- **Главную страницу**: обычно `src/pages/Index.tsx` или `src/pages/Landing.tsx`
- **Компоненты**: файлы в `src/components/`
- **Ассеты**: изображения, видео в `src/assets/` или `public/`
- **Библиотеки**: вспомогательные файлы в `src/lib/`
- **Специальные стили**: кастомные CSS переменные или анимации

### 3️⃣ Копирование файлов

#### A. Страница
```bash
cd "/Users/kikanbig/Контент21/СЕТЫ/Сет вкусный/21vek-dom.ru"
cp "<ПУТЬ_К_РЕПО>/src/pages/Index.tsx" src/pages/<ИМЯ_СТРАНИЦЫ>.tsx
```
**Пример**: `Index.tsx` → `NPS.tsx`, `Landing.tsx` → `Regions.tsx`

#### B. Компоненты
```bash
cp "<ПУТЬ_К_РЕПО>/src/components/<КОМПОНЕНТ>.tsx" src/components/
```

#### C. Библиотеки (если есть)
```bash
mkdir -p src/lib
cp "<ПУТЬ_К_РЕПО>/src/lib/<ФАЙЛ>.ts" src/lib/
```

#### D. Ассеты
```bash
mkdir -p src/assets/<ПАПКА>
cp -r "<ПУТЬ_К_РЕПО>/src/assets/<ПАПКА>/"* src/assets/<ПАПКА>/
```

### 4️⃣ Добавление маршрута в App.tsx

Откройте `src/App.tsx` и добавьте:

```typescript
// В импорты
import <ИМЯ_СТРАНИЦЫ> from "./pages/<ИМЯ_СТРАНИЦЫ>";

// В Routes (ПЕРЕД NotFound!)
<Route path="/<ПУТЬ>" element={<<ИМЯ_СТРАНИЦЫ> />} />
```

**Пример**:
```typescript
import NPS from "./pages/NPS";
// ...
<Route path="/nps" element={<NPS />} />
```

### 5️⃣ Обновление стилей (если нужно)

Если репозиторий использует кастомные CSS переменные или анимации, добавьте их в `src/index.css`:

#### Переменные
```css
:root {
  /* Добавьте новые переменные */
  --heart-empty: 270 20% 85%;
  --heart-filled: 316 95% 29%;
}
```

#### Утилиты и анимации
```css
@layer utilities {
  .custom-class {
    /* стили */
  }
}

@keyframes customAnimation {
  /* анимация */
}
```

### 6️⃣ Сборка и проверка

```bash
npm run build
```

Если есть ошибки — проверьте:
- ✅ Все импорты корректны
- ✅ Все компоненты скопированы
- ✅ Все ассеты на месте

### 7️⃣ Коммит и деплой

```bash
git add -A
git commit -m "✨ Добавлена страница <ИМЯ>: <репозиторий> → /<путь>"
git push origin main
```

Для деплоя на Railway:
```bash
railway login  # если не авторизованы
railway up
```

---

## 🎯 Примеры интеграций

### Пример 1: NPS опрос (heartfelt-feedback)
```bash
# 1. Клонирование
git clone https://github.com/kikanbig/heartfelt-feedback.git

# 2. Копирование
cp heartfelt-feedback/src/pages/Index.tsx src/pages/NPS.tsx
cp heartfelt-feedback/src/components/{NPSSurvey,HeartRating}.tsx src/components/
cp heartfelt-feedback/src/lib/sheets.ts src/lib/
cp heartfelt-feedback/src/assets/logo.png src/assets/

# 3. Маршрут в App.tsx
import NPS from "./pages/NPS";
<Route path="/nps" element={<NPS />} />

# 4. Стили в index.css
--heart-empty: 270 20% 85%;
--heart-filled: 316 95% 29%;
.heart-glow { filter: drop-shadow(...); }
@keyframes heartFill { ... }

# 5. Сборка
npm run build

# 6. Коммит
git add -A && git commit -m "✨ NPS опрос → /nps" && git push
```

### Пример 2: Региональный лендинг (regions)
```bash
# Копирование компонентов landing
cp regions/src/components/landing/*.tsx src/components/landing/

# Копирование Header как RegionsHeader (без навигации)
cp regions/src/components/Header.tsx src/components/RegionsHeader.tsx

# Переименование export в RegionsHeader.tsx
# export const Header → export const RegionsHeader

# Копирование страницы
cp regions/src/pages/Landing.tsx src/pages/Regions.tsx

# Изменение импорта в Regions.tsx
# import { Header } → import { RegionsHeader }
# <Header /> → <RegionsHeader />

# Копирование ассетов
cp -r regions/src/assets/gallery/* src/assets/gallery/
cp -r regions/src/assets/banners/* src/assets/banners/
```

---

## ⚠️ Важные моменты

### 🔴 Кастомные Header
Если страница **НЕ должна** показывать навигацию ("Мебель", "Сеты", "Лоты", "Акции"):

1. Скопируйте `Header.tsx` с новым именем:
   ```bash
   cp <РЕПО>/src/components/Header.tsx src/components/<Страница>Header.tsx
   ```

2. Переименуйте export:
   ```typescript
   // Было: export const Header
   // Стало: export const OpeningHeader (или RegionsHeader, NPSHeader и т.д.)
   ```

3. В файле страницы измените импорт:
   ```typescript
   import { OpeningHeader } from '@/components/OpeningHeader';
   // ...
   <OpeningHeader />
   ```

### 🟢 Общий Header
Если страница **должна** показывать навигацию — используйте стандартный `Header`:
```typescript
import { Header } from '@/components/Header';
<Header />
```

### 🎨 Цвета бренда
Основной цвет проекта: **RGB(145, 3, 105)** = **HSL(316, 95%, 29%)**

При добавлении стилей из нового репозитория адаптируйте цвета под бренд 21vek.by.

---

## 🚀 Быстрый чеклист

- [ ] Репозиторий склонирован
- [ ] Структура изучена (страница, компоненты, ассеты, библиотеки)
- [ ] Файлы скопированы в правильные папки
- [ ] Маршрут добавлен в `App.tsx`
- [ ] Стили добавлены в `index.css` (если нужно)
- [ ] Кастомный Header переименован (если нужно)
- [ ] `npm run build` успешен
- [ ] Изменения закоммичены и запушены
- [ ] Деплой на Railway выполнен

---

## 📚 Структура проекта

```
21vek-dom.ru/
├── src/
│   ├── pages/
│   │   ├── Index.tsx          # Главная (hoff-divan-insights)
│   │   ├── Sets.tsx           # Сеты (sets-repo)
│   │   ├── Opening.tsx        # Открытие магазина (opening)
│   │   ├── Regions.tsx        # Региональные магазины (regions)
│   │   ├── NPS.tsx            # NPS опрос (heartfelt-feedback)
│   │   └── NotFound.tsx       # 404
│   ├── components/
│   │   ├── Header.tsx         # Общий Header С навигацией
│   │   ├── OpeningHeader.tsx  # Header БЕЗ навигации (opening)
│   │   ├── RegionsHeader.tsx  # Header БЕЗ навигации (regions)
│   │   ├── NPSSurvey.tsx      # NPS компонент
│   │   ├── HeartRating.tsx    # Рейтинг сердечками
│   │   └── landing/           # Компоненты для regions
│   ├── lib/
│   │   ├── utils.ts           # Общие утилиты
│   │   └── sheets.ts          # Google Sheets API (NPS)
│   ├── assets/                # Все изображения
│   ├── App.tsx                # Роутинг
│   └── index.css              # Глобальные стили
└── public/
    ├── favicon.png
    └── videos/                # Видео файлы
```

---

## 🔗 Полезные ссылки

- **Проект на Railway**: https://railway.app/project/27e6666f-49d1-4fbb-993c-a861a61af645
- **Домен**: https://dom.21vek.by
- **GitHub**: https://github.com/kikanbig/21vek-dom.ru

---

**Последнее обновление**: 25 декабря 2024


